# Toolchain executables

| File | Purpose |
| --- | --- |
| `toolchain-smoke.rs` | Requires all five Windows manifest tools by default; --allow-legacy-four-tools true permits historical baseline comparison; downloads, hashes, probes executables and writes native smoke report; See change: owned-toolchain. |
