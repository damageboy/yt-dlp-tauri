# Execution gates

1. Generate lock, manifest and changelog for 20260912.1; verify unchanged tool bytes and no historical runtime URLs.
2. Pass local tests and native Windows candidate validation; merge reviewed metadata.
3. Publish all four packages, promote stable channel and publish verified platform installers.
4. Verify live downloads and checksums, then delete five previous toolchain releases and tags.
